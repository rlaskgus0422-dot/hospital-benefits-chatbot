@AGENTS.md

# 프로젝트 규칙 (my-app)

병원 교직원용 복리후생 문의 챗봇 서비스. 전체 기획 배경/범위는 [PRD.md](PRD.md) 참고.

## 기술 스택

- Next.js 16 (App Router, `app/` 디렉터리 — `src/` 폴더 미사용)
- React 19, TypeScript(strict), Tailwind CSS v4
- AI 응답 엔진: OpenAI API (등록된 규정 원문 기반 답변)
- 배포: Vercel

## 작업 규칙 (Claude Code 운영 규칙)

- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 `my-app` 폴더 안에만 만든다.
- 기술 스택은 PRD에 정한 대로 Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 Vercel을 사용한다.
- 코드를 바꾸면 반드시 무엇을, 왜 바꿨는지 한 줄로 알려준다.
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase가 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.

## 작업 절차 (검증 루프)

코드를 변경할 때는 아래 순서를 통과할 때까지 반복한다.

1. 변경한다.
2. `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다.
3. 화면이 바뀐 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다.
4. 문제가 있으면 고치고 다시 1)로 돌아간다.
5. 통과하면 무엇을, 왜 바꿨는지 한 줄로 요약한다.

## AI 답변 동작 규칙 (must-have, 임의로 완화하지 말 것)

- 챗봇 답변은 반드시 관리자가 등록한 공지사항·사내 규정 원문에 근거해서만 생성한다. 근거 문서에 없는 내용은 추측해서 답하지 않는다.
- 근거를 찾지 못했거나 확신할 수 없는 질문에는 임의로 답하지 말고 총무팀 연락처(02-2290-9024)를 안내한다.
- 급여 관련 질문은 총무팀 복리후생 범위가 아니므로 인사팀 문의로 안내한다. 단, 휴가는 인사팀 소관 규정이지만 등록된 규정 원문에 근거해 챗봇이 직접 답변한다.

## 범위 (PRD 6번 기준)

만들지 않는 것: 개인별 복리후생 이용내역 조회/계산, 복리후생 외 업무(급여·인사평가·채용 등) 직접 응대, 일반 회원가입/로그인 시스템, 모바일 앱, 한국어 외 다국어 지원. 새 기능을 추가하기 전 이 목록과 충돌하는지 먼저 확인한다.

## 보안 · 개인정보

- `.env`는 절대 커밋하지 않는다(`.gitignore`에 `.env*` 등록됨). API 키·토큰 등 민감정보는 `.env`에만 두고 코드에 하드코딩하지 않는다.
- 개인 식별 정보는 수집·저장하지 않는다. 질문·답변 로그를 저장하더라도 개인 식별 정보를 포함하지 않는다.
- 관리자 페이지(`/admin` 등)는 총무팀 전용 비밀번호로 접근을 제한하고, 일반 사용자 라우트와 명확히 분리한다.
- (2026-09-04 변경) 병원 내부망 IP 제한은 사용자 요청으로 제거했다. 일반 사용자 화면(챗봇·FAQ)은 인터넷 어디서나 접속 가능하며, 관리자 페이지만 비밀번호로 보호된다. IP 제한 로직(`app/_lib/ipAllowlist.ts`)은 `trash-can/app/_lib/`로 옮겨두었으니, 다시 내부망 전용으로 되돌리려면 그 파일을 복원해 `proxy.ts`에서 다시 호출하면 된다.

## Next.js 16 관련 유의사항

- 이 저장소의 Next.js 버전은 학습 데이터 시점보다 최신이라 API/컨벤션이 다를 수 있다. 새로운 컨벤션을 사용하기 전 `node_modules/next/dist/docs/`의 관련 가이드를 먼저 확인한다 (자세한 내용은 [AGENTS.md](AGENTS.md) 참고).
- `middleware.ts`가 아니라 `proxy.ts`를 사용한다.

## 코드 컨벤션

- API는 Route Handler(`app/**/route.ts`)로 구현한다.
- 라우트 전용 컴포넌트/유틸은 `_components`, `_lib` 같은 프라이빗 폴더에 colocate한다.
- 스타일은 Tailwind CSS 유틸리티 클래스를 사용한다.
- TypeScript strict 모드를 유지하고, 경로 별칭 `@/*`를 사용할 수 있다.

## 개발 명령어

```bash
npm run dev    # 개발 서버
npm run build  # 프로덕션 빌드
npm run start  # 프로덕션 서버 실행
npm run lint   # ESLint 검사
```
