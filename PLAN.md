# PLAN — 복리후생 문의 챗봇 (PRD 기반)

## 이번 사이클 목표

PRD([PRD.md](PRD.md))에 정의된 복리후생 챗봇 MVP(사용자 챗봇 + 관리자 문서 관리 + 안전장치)를 실제로 동작하는 상태까지 구현한다.

## 성공 기준

- 사용자가 카테고리를 선택하거나 질문을 입력하면, 등록된 규정 원문에 근거한 답변을 받는다.
- 근거를 찾지 못한 질문에는 총무팀 연락처(02-2290-9024)가 안내된다.
- 급여·휴가 관련 질문에는 인사팀 문의(내선 9025)로 안내된다.
- 관리자가 비밀번호로 로그인해야만 `/admin` 화면과 업로드·삭제 API에 들어갈 수 있고(쿠키 위조로 우회되지 않는다), PDF 문서를 업로드·삭제할 수 있다.
- 질문·답변 로그가 개인 식별 정보(IP, 사번 등) 없이 저장된다.
- 병원 공인 IP 대역 외의 접속은 차단된다.
- `npm run lint`, `npm run build`가 오류 없이 통과하고 Vercel에 배포된다.

## 작업 순서

1. 챗봇 채팅 UI 뼈대 페이지 만들기 — 이후 모든 기능이 올라갈 기본 화면 틀
2. 복리후생 카테고리 선택 UI 붙이기 — 질문 입력 진입점 완성
3. 관리자 비밀번호 로그인 페이지와 `proxy.ts`(로그인 세션 확인용) 뼈대 만들기 — 문서 관리 기능의 전제 조건
4. Supabase 프로젝트를 만들고 문서 테이블·로그 테이블을 준비한 뒤 접속 정보를 `.env`에 등록하기 — 이후 모든 저장 작업의 전제 조건
5. 관리자 페이지에서 규정 PDF 업로드·목록조회·삭제 기능 만들기 (카테고리당 1건, 4MB 이하, 텍스트 추출 안 되면 거부) — AI가 참고할 원문 데이터 확보
6. 업로드된 규정 원문을 OpenAI API가 답변 근거로 참고하도록 연결하기 (카테고리별 원문을 통째로 프롬프트에 전달) — 실제 답변 생성의 핵심 연결고리
7. 사용자 질문에 규정 원문 근거로 답하는 챗봇 응답 기능 완성하기 (근거 없을 때 `NO_ANSWER` 규칙 포함, 응답 대기·오류 화면 포함)
8. 근거를 찾지 못한 질문에 총무팀 연락처를 안내하는 규칙 추가하기 (`NO_ANSWER` 응답을 감지해 문구로 치환)
9. 급여 관련 질문이면 인사팀 문의(내선 9025)로 안내하는 규칙 추가하기 (미매칭 상태에서 급여 키워드가 있을 때만 인사팀 안내, 그 외 미매칭+키워드 없음은 총무팀 안내 — 휴가/휴직은 인사팀 소관 규정이지만 다른 카테고리와 동일하게 등록된 규정 원문 근거로 챗봇이 직접 답변한다)
10. 개인 식별 정보 없이 질문·답변 로그를 저장하는 기능 만들기 (IP·세션ID·User-Agent 저장 금지, 질문 속 숫자 나열은 마스킹)
11. 전산팀에서 병원 공인 IP 대역을 확보해 `proxy.ts`에 접근 제한 규칙 추가하기
12. Vercel 프로젝트에 5개 환경변수(`OPENAI_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `ADMIN_PASSWORD`, `ALLOWED_IP_RANGES`)를 등록하고 배포한 뒤 최종 점검하기

## 이번 사이클 제외 (보류)

- 자주 묻는 질문(FAQ) 목록 UI — 다음 사이클로 이월
- 관리자용 질문·답변 로그 조회 화면 — MVP에서는 Supabase 콘솔에서 직접 확인
