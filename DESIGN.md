# DESIGN — 복리후생 문의 챗봇 (PLAN 기반)

[PLAN.md](PLAN.md)의 작업 순서를 실제로 구현하기 위한 화면 구성, 데이터 흐름, 기술 선택을 정리한다.

## 1. 화면 구성 (무엇이 어디에 보이는지)

### 1-1. 메인 챗봇 화면 (`/`)

```
┌─────────────────────────────────────┐
│  복리후생 문의 챗봇 (제목/안내문구)      │
├─────────────────────────────────────┤
│ [경조금][화환][위탁보육료][휴양소]      │  ← 카테고리 버튼 (클릭 시 질문창에 자동 입력)
│ [동아리지원금][휴가][주차][식대]        │
│ 선택된 카테고리: 경조금 ✕               │  ← 버튼을 누르면 표시되는 칩(뱃지)
├─────────────────────────────────────┤
│  (챗봇) 안녕하세요, 무엇을 도와드릴까요? │
│  (사용자) 경조금은 얼마인가요?          │  ← 대화창 (말풍선 나열)
│  (챗봇) 등록된 규정에 따르면...         │
│  (챗봇) 답을 찾지 못했어요. 총무팀      │
│  (02-2290-9024)로 문의해주세요.        │
├─────────────────────────────────────┤
│ [질문 입력창.......................] [전송] │
└─────────────────────────────────────┘
```

- 카테고리 버튼을 누르면 입력창에 문구가 채워지고 위에 "선택된 카테고리" 칩이 뜬다. **입력창 글자를 직접 고치면 칩이 사라지고 카테고리 선택은 해제된다** — 자동 입력된 문구를 지우고 다른 질문을 썼는데 예전 카테고리로 답이 나오는 걸 막기 위함.
- **휴가 카테고리는 예외**: 8개 카테고리 버튼 중 하나로 계속 남아있지만, 눌러도 규정 답변이나 FAQ를 만들지 않고 "휴가 관련 문의는 인사팀으로 연락해주세요. (내선 9025)"만 표시한다. 직접 입력한 질문이 휴가로 분류될 때도 동일하다.
- 급여 관련 질문이면 챗봇 답변 대신 "인사팀(내선 9025)에 문의해주세요" 문구를 표시한다.
- 근거를 못 찾으면(또는 등록된 문서가 아직 없으면) 챗봇 답변 대신 총무팀 연락처를 표시한다.
- **응답 대기 중**: 전송 직후 입력창과 전송 버튼을 비활성화하고, "답변 작성 중..." 말풍선을 보여준다. 응답이 오기 전 중복 전송을 막는다.
- **오류 시**: OpenAI 호출 실패·타임아웃이면 "일시적인 오류입니다. 총무팀(02-2290-9024)으로 문의해주세요"를 표시한다.
- 모든 답변은 항상 한국어로만 표시한다(질문이 외국어여도 한국어로 답변).

### 1-2. 관리자 로그인 화면 (`/admin/login`)

```
┌───────────────────┐
│   관리자 로그인      │
│  [비밀번호 입력창]    │
│      [로그인]        │
└───────────────────┘
```

- 메인 화면과 링크로 연결하지 않고, 주소를 직접 입력해야 들어갈 수 있게 한다.
- 비밀번호가 틀리면 "비밀번호가 올바르지 않습니다"를 표시한다.

### 1-3. 관리자 대시보드 (`/admin`)

```
┌─────────────────────────────────────┐
│  규정 문서 관리                        │
├─────────────────────────────────────┤
│  [카테고리 선택 ▾] [PDF 파일 선택] [등록] │  ← 새 문서 업로드
├─────────────────────────────────────┤
│  경조금 - 경조금규정.pdf   [삭제]       │
│  휴가   - 휴가규정_2026.pdf [삭제]      │  ← 등록된 문서 목록 (파일명만 표시, 다운로드 링크는 노출하지 않음)
│  (등록된 문서가 없습니다)               │  ← 0건일 때 표시
└─────────────────────────────────────┘
```

- 카테고리 하나당 문서는 **1건만** 허용한다. 이미 등록된 카테고리를 다시 올리려 하면 "이미 등록된 문서가 있어요. 먼저 기존 문서를 삭제해주세요"를 표시한다. "수정"은 기존 문서를 삭제한 뒤 같은 카테고리로 다시 업로드하는 방식으로 대신한다.
- 업로드 제한: PDF만 허용, 파일 크기 4MB 이하. 초과·형식 오류 시 그 자리에서 거부하고 이유를 표시한다.
- 업로드한 PDF에서 텍스트가 거의 추출되지 않으면(스캔본 이미지 PDF 등) "텍스트를 추출할 수 없는 PDF입니다"로 거부한다.
- 질문·답변 로그 조회 화면은 이번 사이클에서 만들지 않는다. 확인이 필요하면 Supabase 콘솔에서 `chat_logs` 테이블을 직접 본다.

## 2. 데이터 흐름 (입력 → 처리 → 출력)

### 2-1. 사용자 질문 흐름

1. **입력**: 사용자가 카테고리를 누르거나 질문을 직접 입력하고 전송한다.
2. **개인정보 마스킹(가장 먼저 처리)**: 서버가 질문을 받자마자, 6자리 이상 이어지는 숫자(전화번호·사번으로 추정)를 `****`로 가린다. 이후 단계(OpenAI 전송, 로그 저장)는 모두 이 마스킹된 문장을 사용한다. (4자리 숫자까지 가리면 "9024" 같은 정상 안내번호나 연도까지 지워지므로 6자리 이상만 가린다.)
3. **분류**: 사용자가 버튼을 눌러 카테고리를 이미 정했으면 그 카테고리를 쓰고, 직접 입력한 질문이면 OpenAI에게 "8개 카테고리 중 하나 또는 '해당없음'으로만 분류하라"고 시켜 분류한다. (이 분류 1회 + 아래 답변 생성 1회로 OpenAI를 최대 2번 호출하며, 버튼으로 진입하면 분류를 건너뛰어 1회로 줄어든다.)
4. **다섯 갈래 처리**:
   - **(a) 카테고리가 "휴가"로 매칭됨**: 문서 존재 여부와 상관없이 OpenAI를 호출하지 않고 바로 인사팀 안내(내선 9025)로 응답한다. 휴가는 급여와 동일하게 취급하는 예외 카테고리다.
   - **(b) 휴가 외 카테고리 매칭 + 해당 카테고리 문서 있음**: 그 문서 원문 전체(너무 길면 앞 12,000자까지만)를 질문과 함께 OpenAI API에 전달해 답을 만든다. 여러 문서 중 일부만 찾아오는 정교한 검색 기능은 이번 사이클에서 만들지 않는다.
   - **(c) 휴가 외 카테고리 매칭 + 해당 카테고리 문서 없음(0건)**: OpenAI를 호출하지 않고 바로 총무팀 안내로 응답한다.
   - **(d) 카테고리 미매칭 + 급여 관련 키워드(급여, 월급, 연봉, 수당, 세금, 원천징수 등) 포함**: OpenAI를 호출하지 않고 바로 인사팀 안내(내선 9025)로 응답한다.
   - **(e) 카테고리 미매칭 + 급여 키워드도 없음**: OpenAI를 호출하지 않고 바로 총무팀 안내로 응답한다. (근거 없이 임의로 답하지 않는다는 PRD 원칙을 지키기 위함)
5. **시스템 프롬프트 규칙** ((b) 경로에서만 사용): "등록된 규정 원문에 근거를 찾을 수 없으면 다른 말을 덧붙이지 말고 정확히 `NO_ANSWER`만 출력하라. 답변은 항상 한국어로 작성하라."
6. **NO_ANSWER 판정**: 서버는 OpenAI 응답에서 앞뒤 공백·따옴표·마침표를 제거한 뒤 `NO_ANSWER`와 같은지 비교한다. 같거나, 응답 문자열 안에 `NO_ANSWER`가 포함돼 있으면(모델이 다른 말을 덧붙인 경우의 안전망) 총무팀 연락처 문구로 바꿔서 보여준다 — 화면에 `NO_ANSWER` 텍스트가 그대로 노출되는 일이 없도록 한다.
7. **로그 저장**: 위 (a)~(e) 및 오류 상황 모두 `chat_logs`에 저장한다(§2-3 참고). 접속 IP·세션ID·User-Agent는 저장하지 않는다.
8. **출력**: 화면에 결과가 표시된다. (b)는 규정 기반 답변, (c)·(e)는 총무팀 안내, (a)·(d)는 인사팀 안내(내선 9025).

> 참고: 휴가 카테고리로 문서를 등록해도(관리자 화면에서 막지는 않음) 위 (a) 규칙 때문에 챗봇 답변에는 쓰이지 않는다.

### 2-2. 관리자 문서 등록 흐름

1. **입력**: 관리자가 비밀번호를 입력해 로그인하고, 카테고리를 선택해 PDF 파일을 업로드한다.
2. **로그인 처리**:
   - 서버가 입력한 비밀번호를 환경변수 `ADMIN_PASSWORD`와 비교한다.
   - 맞으면 서버가 무작위 세션 값을 만들고, `ADMIN_PASSWORD`를 비밀키로 사용해 서명(HMAC)한 값을 `admin_session` 쿠키에 담아 발급한다(httpOnly, secure, sameSite=strict, 유효시간 8시간). **단순히 쿠키가 "있는지"가 아니라 서명 값이 올바른지까지 검사**하므로, 쿠키를 임의로 조작해 넣는 방식으로는 로그인을 우회할 수 없다.
3. **업로드 처리**:
   - 카테고리에 이미 문서가 있으면 거부한다(§1-3).
   - PDF가 아니거나 4MB를 넘으면 그 자리에서 거부한다.
   - 서버가 PDF 파일을 Supabase Storage의 **private 버킷**에 저장하고(공개 URL로 직접 접근 불가, 서버가 `SUPABASE_SERVICE_ROLE_KEY`로만 접근), PDF 텍스트 추출 도구로 글자를 뽑아낸다.
   - 추출된 텍스트가 너무 짧으면(예: 50자 미만) 스캔본으로 간주해 업로드를 거부한다.
   - 통과하면 카테고리·파일 정보와 함께 `documents` 테이블에 저장한다.
   - 삭제 버튼을 누르면 Storage 파일과 `documents` 행을 함께 지운다.
4. **출력**: 관리자 화면의 문서 목록에 새 문서가 추가되거나, 삭제한 문서가 목록에서 사라진다.

### 2-3. 데이터 저장 구조 (최소 스키마)

이후 카테고리 관리·질문 관리·FAQ 답변·담당자 안내 기능이 추가되면서 아래 4개 테이블이 더 생겼다(전체 SQL은 [supabase/migrations/0001_init.sql](supabase/migrations/0001_init.sql) 참고). `category` 이름 변경/삭제는 외래키의 `on update cascade` / `on delete cascade`(또는 `set null`)로 관련 테이블에 자동 전파된다.

- **`categories`** (카테고리 목록과 표시 순서): `name`(기본키), `position`(정렬 순서), `created_at`
- **`category_questions`** (카테고리별 질문 목록과 순서): `category`(→ `categories.name`), `question`, `position` — 기본키 `(category, question)`
- **`faq_answers`** (질문별 등록 답변, 비어있으면 규정 문서 근거 답변으로 대체): `category`(→ `categories.name`), `question`, `answer` — 기본키 `(category, question)`
- **`contact_messages`** (카테고리별 담당자 안내 문구, 없으면 공통 총무팀 문구 사용): `category`(기본키, → `categories.name`), `message`
- **`documents`** (등록된 규정 원문, 카테고리당 최대 1행): `id`, `category`(→ `categories.name`, unique), `file_name`, `content_text`(추출된 텍스트), `storage_path`(Storage 파일 위치), `created_at`
- **`chat_logs`** (질문·답변 로그): `id`, `question`(마스킹 적용된 문장), `answer`, `matched_category`(분류 결과, 없으면 null, → `categories.name` on delete set null), `answer_type`(`answered` | `no_answer` | `hr_referral` | `error` 중 하나 — §2-1의 (b)/(c)·(e)/(a)·(d)/오류에 대응), `feedback`(`helpful` | `unhelpful` | null), `created_at`

### 2-4. 접근 제한 흐름 (`proxy.ts`, 파일 1개)

Next.js는 프로젝트당 `proxy.ts` 파일을 1개만 지원하므로, 아래 검사를 한 파일 안에서 순서대로 처리한다. `matcher`는 `['/admin/:path*', '/api/admin/:path*']`로 지정해 관리자 화면과 **관리자 API(업로드·목록·삭제)를 모두** 포함한다(일반 챗봇 API `/api/chat`는 로그인 검사 대상이 아니다).

1. **IP 허용 검사** (모든 경로, matcher 없이 전체 적용): 요청 헤더의 `x-forwarded-for` 중 **첫 번째** 값을 `ALLOWED_IP_RANGES`와 비교한다.
   - 값 형식: 콤마로 구분한 CIDR 목록 (예: `210.94.10.0/24,203.0.113.5/32`).
   - **개발 중 예외**: `NODE_ENV`가 `production`이 아니거나 `ALLOWED_IP_RANGES`가 비어 있으면 이 검사를 건너뛴다 — 그래야 로컬(`npm run dev`)에서 본인 PC로 계속 개발할 수 있다.
2. **관리자 세션 검사** (matcher로 지정한 `/admin/:path*`, `/api/admin/:path*` 중 `/admin/login`은 제외): `admin_session` 쿠키의 서명 값이 올바른지 검사한다. 실패하면 화면 요청은 `/admin/login`으로 돌려보내고, API 요청은 401 오류를 응답한다.
3. **이중 방어**: proxy 통과 여부와 별개로, 업로드·삭제를 처리하는 각 Route Handler 안에서도 같은 쿠키 검사를 한 번 더 한다(Next.js 공식 권고 — proxy만으로 인가를 전부 맡기지 않는다).

## 3. 기술 선택

**기본(확정, PRD 기준)**

- **Next.js**: 화면과 서버 코드를 한 프로젝트에서 같이 만들 수 있는 도구. Part 5에서 Vercel로 그대로 배포한다.
- **OpenAI API**: 사용자 질문에 대한 답변 문장을 만들어주는 AI 두뇌 역할.
- **Vercel**: 완성된 웹사이트를 인터넷에 올려서 누구나 접속할 수 있게 해주는 배포 서비스.

**추가 제안 (역할과 이유)**

- **Supabase**: 규정 원문 텍스트와 질문·답변 로그를 표(엑셀 시트 같은 형태)로 저장하는 인터넷 데이터베이스. `.env`의 `SUPABASE_ACCESS_TOKEN`은 계정 관리용 토큰이라 앱에서 바로 쓸 수는 없고, Supabase 홈페이지에서 프로젝트를 하나 만들면 앱 연결용 주소(URL)와 키를 별도로 받아야 한다.
- **Supabase Storage**: 관리자가 올린 PDF 원본 파일 자체를 보관하는 파일 창고(비공개 버킷으로 생성). Supabase 안에 포함된 기능이라 별도 가입 없이 같이 쓴다.
- **PDF 텍스트 추출 도구 (예: pdf-parse)**: PDF 파일 속 글자를 컴퓨터가 읽고 검색할 수 있는 순수 텍스트로 뽑아내는 작은 프로그램. 이번 사이클은 PDF만 지원하고, hwp·docx 등은 다음 사이클로 미룬다.
- **Next.js Proxy (`proxy.ts`)**: 별도 설치 없이 Next.js에 이미 들어있는 기능으로, 병원 IP가 아니면 접속을 막고 관리자 로그인 여부도 함께 확인하는 문지기 역할을 한다.

### 필요한 환경변수 (`.env`, 배포 시 Vercel 프로젝트 설정에도 동일하게 등록)

| 변수명 | 용도 | 비고 |
|---|---|---|
| `OPENAI_API_KEY` | 챗봇 답변 생성 | 이미 `.env`에 있음 |
| `SUPABASE_URL` | Supabase 프로젝트 주소 | Supabase에서 프로젝트 생성 후 새로 발급. 서버에서만 쓰므로 `NEXT_PUBLIC_` 접두사를 붙이지 않는다 |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버에서만 쓰는 Supabase 관리자 키 | 절대 화면 코드에 노출하지 않음 |
| `ADMIN_PASSWORD` | 관리자 로그인 비밀번호이자 세션 쿠키 서명 비밀키 | 새로 정해서 등록 (별도 서명용 키를 추가하지 않고 재사용해 변수 개수를 줄임) |
| `ALLOWED_IP_RANGES` | 병원 허용 IP 대역 (콤마 구분 CIDR) | 전산팀에 확인 후 등록. 비워두면 개발 중 IP 검사가 꺼짐 |
